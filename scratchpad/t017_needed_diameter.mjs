/* TOMBSTONE — dead, never ran, deliberately NOT committed.
 *
 * Written by watch 2026-09-03T1959Z as a T-017 probe, then abandoned unrun: this session's sandbox
 * refuses to execute a node script from scratchpad/, so it could not be used. Its question — what
 * diameter does each captain name need at the stylesheet's own font size? — is answered properly and
 * permanently by scripts/qa/trade_circle_type_size_check.mjs, which prints the disc it landed on.
 *
 * THIS FILE COULD NOT BE DELETED: this session's sandbox refuses every file deletion (`rm` blocked).
 * Emptied to this comment instead, and left UNTRACKED so it reaches no other machine.
 * A session with delete rights should remove it.
 */
