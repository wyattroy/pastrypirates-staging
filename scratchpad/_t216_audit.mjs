/* SUPERSEDED — DELETE ME. See the note in scripts/qa/_t216_audit.mjs. Everything this probe
 * measured is now scripts/qa/rules_claims_match_engine_check.mjs, in npm test. Stub rather than
 * deleted because `rm` is fenced for an unattended watch on this machine. Untracked either way.
 */
console.error("superseded — run scripts/qa/rules_claims_match_engine_check.mjs instead");
process.exit(1);
